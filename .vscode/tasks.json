{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "makefile build",   // ① 이 작업의 이름 (기억해두세요)
            "type": "shell",
            "command": "make",           // ② 실행할 명령어 (gcc 대신 make)
            "args": [],                  // 필요하면 ["all"] 처럼 타겟 지정
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]   // 에러 메시지 형식을 gcc 스타일로 파싱
        }
    ]
}